## Validation: issue #8 — PASS
Spec-anchored check: 3/3 ACs matched (CR-01 in spec.md: ChatCase rejects empty conversation -> ValidationError asserted; ChatCase.criteria defaults to None -> value asserted; ChatResult.tool_called defaults to None on text-only reply -> value asserted). No spec-precision gaps.
Mutation sensor: 1 injected (Common tier: conversation Field min_length=1 -> min_length=0, primary happy path), 1 killed, 0 survived.
Report: .specs/features/chateval-lina/validation.md

## Validation: issue #11 — PASS
Spec-anchored check: 5/5 ACs matched (CR-04 in spec.md: ChatTarget subprocess call, JSON stdin/stdout contract, no lina-mvp import, error naming mirrors openai_compat.TargetRuntimeError, tests use injected fake runner only. spec.md cross-references plan Task 5's verbatim code/tests rather than defining separate precise values, so no gap between spec and test assertions).
Mutation sensor: 1 injected (Common tier, primary happy path: flipped `completed.returncode != 0` to `== 0` in chat_target.py, applied only in scratch/restored immediately), 1 killed, 0 survived.
Reviewer: independent Haiku 4.5 pass, spec-compliance only, PASS, no blocking findings, no scope creep (exactly 2 new files).

## Validation: issue #10 — FAIL
Spec-anchored check: 6/6 ACs matched by value (pyproject.toml deepeval>=2.0 added; ChatJudge.score returns exact dict {"tool_selection_accuracy": 1.0} for tool-only case and adds scores["tone_brand"] == 0.95 for criteria case, both asserted as literal values not just presence; ChatJudgeError/ChatJudgeRuntimeError mirror ollama.py's JudgeError/JudgeRuntimeError base+Runtime shape; gate green 158 passed, ruff clean; domain/chat.py untouched, no cli.py/targets/runner/config touched, diff --stat covers only the 3 expected files). 1 gap flagged: `_criteria_metric_name`'s tone_brand-vs-hallucination selection heuristic (string-match on "tool" in case.criteria + "hallucin"/"false" in case.id) is a maker judgment call with no basis in spec.md's CR-03 row, which only says "GEval (tone/brand, hallucination)" without a selection rule — should be pinned down in spec.md or an ADR before more cases are added to the 17-case dataset, since the heuristic is stringly-typed and easy to silently break.
Mutation sensor: 6 injected, 4 killed, 2 survived: (1) `if case.criteria` -> `if not case.criteria` KILLED all 3 tests; (2) `_criteria_metric_name` default `"tone_brand"` -> `"hallucination"` KILLED test_case_with_criteria_also_scores_a_geval_metric; (3) `_score_tool_selection` actual_tools forced to `[]` (dropping the real tool call) SURVIVED all 3 tests in tests/unit/test_chat_judge.py; (4) except-clause `raise ChatJudgeRuntimeError` -> `return {}` (swallow) KILLED test_both_providers_failing_raises_chat_judge_runtime_error; (5) `_score_tool_selection` `float(metric.score)` -> `float(metric.score) + 1` KILLED 2 tests; (6) `expected_tools=expected_tools` -> `expected_tools=[]` in the LLMTestCase constructed for tool selection SURVIVED all 3 tests. Both survivors share one root cause: StubToolMetric/StubGEval.measure() record `measured_with` but no test in tests/unit/test_chat_judge.py ever asserts on it — only the returned score value is checked, so the entire LLMTestCase wiring (tools_called, expected_tools, input, actual_output) is unverified. BLOCKING: add an assertion on `StubToolMetric.measured_with.tools_called == [ToolCall(name="answer_question")]` and `.expected_tools == [ToolCall(name="answer_question")]` (or equivalent) to test_tool_selection_case_only_scores_tool_selection_accuracy before this passes quench.
Report: .specs/features/chateval-lina/validation.md

## Validation: issue #10 (fix round 2) — PASS
Fix applied: test_tool_selection_case_only_scores_tool_selection_accuracy now captures the StubToolMetric instance and asserts `[tc.name for tc in stub.measured_with.tools_called] == ["answer_question"]` and the same for `expected_tools`; test_case_with_criteria_also_scores_a_geval_metric asserts the empty-tools case symmetrically. src/gnomon/judge/chat_judge.py unchanged (verified byte-identical, only tests/unit/test_chat_judge.py touched by this round).
Mutation sensor re-check: both previously-surviving mutations (actual_tools forced to [], expected_tools forced to []) now KILLED. 0 survivors remain.
Code-quality reviewer (Opus, independent, WARN non-blocking): flagged `_criteria_metric_name`'s string-match heuristic ("tool" in criteria + "hallucin"/"false" in case.id) as fragile/silent-mis-tag risk in the judge risk area — carried into RULES.md "Proposed by the loop" via anneal, not a blocker for this pass.
Gate: ruff check clean, ruff format clean, pytest 158 passed.
Report: .specs/features/chateval-lina/validation.md

## Validation: issue #12 — PASS
Spec-anchored check: 3/3 ACs matched (CR-05 in spec.md: `run_chat_eval(cases, target, judge, *, seed, confidence_level=0.95) -> EvalReport` signature exact; test asserts literal values `metric.n == 2`, `metric.mean == 0.5`, `report.total_tokens == 30`, `report.mean_latency_ms == 150.0` -- not just presence of assertions; `confidence.py`/`gate.py`/`domain/models.py` confirmed untouched via `git diff --stat`, only 2 new files). No spec-precision gaps.
Mutation sensor: 1 injected (Common tier, primary happy path: `scores_by_metric.setdefault(metric_name, []).append(value)` -> `scores_by_metric[metric_name] = [value]`, dropping the per-case accumulation side effect), 1 killed (test fails: aggregate_metric raises "need at least 2 cases" since only the last case's score survives), 0 survived. Mutation applied to a backed-up copy of the file and restored byte-identical immediately after.
Reviewer: independent Haiku 4.5 pass, spec-compliance only, PASS, no blocking findings, no scope creep (exactly 2 new files, signature exact, aggregate_metric/EvalReport/MetricResult/CaseCost reused unchanged).
Gate: ruff check clean, ruff format clean, pytest 169 passed (168 + 1 new).
Report: .specs/features/chateval-lina/validation.md

## Validation: issue #13 — PASS
Spec-anchored check: 8/8 ACs matched by value (CR-06/NFR-02 in spec.md: `ChatRunConfig.from_file` asserts every field of all 4 TOML sections, not just one per section; `config/chat.toml` asserted byte-for-byte against the real committed file via `test_real_chat_toml_matches_spec` (primary_model, fallback_model, all 3 gate thresholds); `--pilot` slicing asserted as literal `len==5` vs `len==7` from a 7-case sentinel list; `chat_main` exit code parametrized `(gate.passed=True,0),(False,1)`; `pyproject.toml` litellm>=1.40 confirmed in diff; signed assumption A2 (`nvidia_nim/<model>` prefix) asserted as the literal constructed kwarg `record[0]["model"] == "nvidia_nim/meta/llama-3.3-70b-instruct"`, not just a shape check). No spec-precision gaps found — every AC in the issue defines an exact expected value and the tests assert that exact value.
Mutation sensor: 6 injected (Legendary tier, risk_area_hit=judge+gate, exceeds the 5+ minimum): (1) `--pilot` off-by-one `cases[:5]`->`cases[:6]` KILLED test_chat_pilot_slices_cases_to_five; (2) gate exit-code flip `0 if passed else 1`->`1 if passed else 0` KILLED both parametrized cases of test_chat_main_exit_code_follows_gate; (3) NIM prefix corruption `nvidia_nim/`->`nim/` KILLED test_judge_model_uses_nim_when_ok; (4) dropped `api_base=` kwarg in the Ollama fallback call KILLED test_judge_model_falls_back_to_ollama_on_nim_failure (this is the RULES.md issue-#10 lesson — assert constructed args, not just the returned string — verified effective here); (5) removed the NIM-try/Ollama-fallback `try/except` entirely KILLED test_judge_model_falls_back_to_ollama_on_nim_failure; (6) inverted `ChatGateConfig`'s threshold-range validator condition KILLED 2/3 of test_chat_config.py (the third, the out-of-range test, still passed but for the wrong reason — validator raised on the *other* in-range thresholds in the same dict, not the intended out-of-range one; not a surviving mutant since the mutation was still killed, but noted as a minor test-precision observation, not blocking). 0 true survivors. All mutations applied to backed-up file copies and restored byte-identical (diff verified empty) immediately after each check; real worktree never left mutated.
Reviewers: two independent passes (Legendary tier) — spec reviewer (Sonnet 4.6): APPROVE, all 8 ACs PASS with value-level assertions, anti-spec-gaming check clean (only 2 new test files, no existing test modified), zero real network calls confirmed (all NIM/Ollama tests inject via `monkeypatch.setitem(sys.modules, "litellm", ...)`), one non-blocking scope note (test_chat_cli.py isn't literally named in the AC list but only strengthens already-mandated behavior). Code-quality reviewer (Opus 4.8): APPROVE, no CRITICAL/blocking findings; one HIGH (non-blocking, judge risk area) — `_build_judge_model`'s NIM-then-Ollama fallback silently swaps providers on any exception with no log signal, risking an unnoticed full-fallback run; one MEDIUM — hardcoded `seed=42` in `run_chat_from_config`, inconsistent with the RAG/session arms' configurable seed; confirmed ADR-006 gate semantics (`ci_low`) reused unchanged, no credential/security issue, local `import litellm` inside methods confirmed deliberate (required for the `sys.modules` test-injection strategy).
Gate: ruff check clean, ruff format clean, pytest 179 passed (169 + 10 new).
Report: .specs/features/chateval-lina/validation.md
